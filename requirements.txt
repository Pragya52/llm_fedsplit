# Core dependencies
torch>=2.0.0
transformers>=4.30.0
numpy>=1.24.0
wandb>=0.15.0

# Data handling
pandas>=2.0.0

# Utilities
tqdm>=4.65.0
dataclasses-json>=0.5.0

# Optional for actual LLaMA model usage (if available)
accelerate>=0.20.0
bitsandbytes>=0.39.0  # For 8-bit quantization

# Development tools (optional)
pytest>=7.3.0
black>=23.3.0
flake8>=6.0.0
